name: 🗓 Info precios diario

on:
  schedule:
    - cron: '0 13 * * *'  # Ejecuta todos los días a las 13:00 UTC (10:00 ARG)
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
    env:
      NODE_ENV: production
      BASE_URL:    ${{ secrets.BASE_URL }}
      AUTH_URL:    ${{ secrets.AUTH_URL }}
      VYJ_USER:    ${{ secrets.VYJ_USER }}
      VYJ_PASS:    ${{ secrets.VYJ_PASS }}
      SMTP_HOST:   ${{ secrets.SMTP_HOST }}
      SMTP_PORT:   ${{ secrets.SMTP_PORT }}
      SMTP_USER:   ${{ secrets.SMTP_USER }}
      SMTP_PASS:   ${{ secrets.SMTP_PASS }}
      FROM_EMAIL:  ${{ secrets.FROM_EMAIL }}
      TO_EMAIL:    ${{ secrets.TO_EMAIL }}
      TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}


    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Instalar dependencias (server)
        run: npm install
        working-directory: server

      - name: Ejecutar scraper y enviar mail
        run: npm run scrape
        working-directory: server

      - name: Commit y push de lastPrices.json
        run: |
          cd server
          git add lastPrices.json
          git diff --quiet --cached || (
            git config user.name "github-actions[bot]" &&
            git config user.email "actions@github.com" &&
            git commit -m "ci: actualizar snapshot de precios" &&
            git push
          )

      - name: Subir PDF como artefacto
        uses: actions/upload-artifact@v4
        with:
          name: reporte-pdf
          path: server/reports/latest.pdf

      - name: Subir Excel como artefacto
        uses: actions/upload-artifact@v4
        with:
          name: reporte-xlsx
          path: server/reports/latest.xlsx

      - name: Subir capturas de error si hay
        if: always()  # Ejecuta incluso si el scraper falló
        uses: actions/upload-artifact@v4
        with:
          name: debug-screenshots
          path: server/debug-page-*.png
